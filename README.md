# A Simple Stack Ordering Skeleton

A skeleton project to make it easier to practice TDD for a simple stack ordering

Run the following commands to get started:

```
yarn
npx nodemon
```

Then start developing
